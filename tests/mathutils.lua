-- ---------------------------------------------------------------------------
-- mathutils.lua — Module test file for Lua require/module support
-- ---------------------------------------------------------------------------

module mathutils

function doubleVal(x: number): number
  return x * 2
end

function triple(x: number): number
  return x * 3
end
