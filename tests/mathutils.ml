// ---------------------------------------------------------------------------
// mathutils.ml — Module test file for MyLang use/mod support
// ---------------------------------------------------------------------------

mod mathutils;

fn doubleVal(x: int) -> int {
  return x * 2;
}

fn triple(x: int) -> int {
  return x * 3;
}
