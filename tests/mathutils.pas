// ---------------------------------------------------------------------------
// mathutils.pas — Unit test file for Pascal uses/unit support
// ---------------------------------------------------------------------------

unit mathutils;

function doubleVal(x: integer): integer;
  Result := x * 2;
end

function triple(x: integer): integer;
  Result := x * 3;
end
